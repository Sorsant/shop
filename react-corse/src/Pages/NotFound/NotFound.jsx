import React from 'react'
import Layout from '../../Components/Layout/Layout'

const NotFound = () => {
    return (
        <Layout>
            error
        </Layout>
    )
}

export default NotFound
